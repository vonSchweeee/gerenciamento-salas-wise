<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gerenciamentodesalas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activity.ExcluirAgendamentoActivity"></activity>
        <activity android:name=".ui.activity.CriacaoAgendamentoActivity" />
        <activity android:name=".ui.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.AgendamentoActivity" />
        <activity android:name=".ui.activity.ListaSalasActivity" />
        <activity android:name=".ui.activity.RegistroActivity" />
        <activity android:name=".ui.activity.CalendarioAgendamentoActivity" />
    </application>

</manifest>